//TODO: add test using redis-mock instead of redis service
//import Redis from 'ioredis-mock'

describe('BookEventService tests', () => {
    it('should post correctly formed book into the Redis queue', () => {})
})